import { HighlightSalaryDirective } from './highlight-salary.directive';

describe('HighlightSalaryDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightSalaryDirective();
    expect(directive).toBeTruthy();
  });
});
